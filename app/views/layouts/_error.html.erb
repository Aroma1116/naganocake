<% if obj.errors.any? %>
  <%= obj.errors.count %>errors prohibited this obj from being saved:
  <ul>
    <% obj.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>